fastapi
uvicorn[standard]
pydantic

langgraph
langchain-core

lancedb
rank-bm25

# CPU-only PyTorch build — the deployed container has no GPU, and the
# default CUDA-enabled torch build loads ~1GB+ of unused GPU libraries
# into memory, contributing directly to the Render free-tier OOM.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers

groq
loguru
python-dotenv